#!/bin/sh

# Nome curto, fly-friendly. Sem fqdn, sem cookie dance.
# Apps sqlite single-node não precisam de clustering — nome curto basta.
export RELEASE_DISTRIBUTION=name
export RELEASE_NODE="${RELEASE_NAME}@127.0.0.1"
